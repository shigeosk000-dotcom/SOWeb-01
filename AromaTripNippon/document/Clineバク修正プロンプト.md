Spring Boot のバグ修正を行ってください。

症状:
- /orders/{id} API で存在しないIDを指定すると 500 が返る
- 期待値は 404

やってほしいこと:
1. 原因箇所を特定
2. 影響範囲を整理
3. 最小差分で修正
4. 関連テストを追加または修正
5. 最後に変更内容を要約

制約:
- Controller にビジネスロジックを書かない
- 例外処理は既存の GlobalExceptionHandler に合わせる
- 既存命名規則を守る